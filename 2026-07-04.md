# Skills Trial Report — 2026-07-04

**Tester**: Hermes Subagent
**Repo**: adminlove520/hermes-evolution
**Branch**: skills-trials

---

## 试用心得：hunt-sqli

本次在 Linux 环境（5.15.0-69-generic，Root）下对 `hunt-sqli` 技能进行了试用体验。

### 技能概述
`hunt-sqli` 是基于 12 篇公开 Writeup 构建的 SQL 注入漏洞挖掘技能，覆盖了盲注、联合注入、报错注入、宽字节注入等多种注入形态。

### 试用过程
按照 Skill 指南加载后，对目标靶场应用进行了 SQL 注入探测。技能提供了清晰的检测向量列表和注入Payload推荐顺序：先用闭合测试 `' or '1'='1` 确认注入点，再用 `UNION SELECT` 探测列数，最后逐步枚举数据。步骤逻辑清晰，不容易走弯路。

### 优点
- Payload 覆盖全面，从基础闭合到高级注入均有涉及
- 分步骤指引降低了误判概率
- 对不同数据库（MySQL、MSSQL、PostgreSQL）的差异有区分

### 不足
- 对自动化工具辅助（如 sqlmap 参数推荐）提及较少
- 无明确"验证根因后再报"的防误报指引

### 总体评价
★★★☆☆（4/5）
适合有一定渗透测试基础的操作者使用，可作为快速参考手册。

## 相关文件
- 报告日期：2026-07-04
- 测试环境：Linux
