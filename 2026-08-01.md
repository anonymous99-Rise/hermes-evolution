# Skills Trial Report — 2026-08-01

**Tester**: Hermes Subagent
**Repo**: adminlove520/hermes-evolution
**Branch**: skills-trials

---

## 试用心得：offensive-osint

### 优点
1. **结构清晰**：SKILL.md 主文件仅 ~400 行，把大量细节下放到 15 个 `references/` 子文件，按需加载，不污染 context。
2. **索引完备**：References Index 表格把每个子文件的用途 + 触发关键词都列清楚了，用之前先查表，不用全文扫读。
3. **内容全面**：覆盖 subdomain enum、GraphQL/Swagger 发现、云 bucket 枚举、身份结构（Entra/Okta/ADFS）、48 模式 secret 正则目录、dork 语料、Shodan/Censys、漏洞优先级（EPSS/KEV）、CI/CD 暴露面、ICS/SCADA 行业专用等。
4. **攻击路径提示模板**（§39）：CRITICAL/HIGH 端点发现后，直接给出攻击路径建议（IDOR、GraphQL 枚举、CORS 窃取、/actuator/env 等），非常实用。
5. **Severity Matrix**（§40）：80+ 案例把观察条件映射到 Severity，减少主观拍脑袋。
6. **Self-Test**（§49）：40 个自测问题，覆盖所有新加内容，方便验证 skill 是否正确加载。

### 缺点 / 可改进
1. **内容量巨大**：15 个 references 文件 + 大量内联内容，首次加载仍需要读取多个文件才能完整使用。建议在 SKILL.md 头部加一个 **"Quick Start"** 小节，列出 3-5 个最常见的"查这个->读那个"对应关系，降低首次使用门槛。
2. **触发关键词大小写敏感**：例如 "identity fabric" 可以触发，但 "Identity Fabric" 可能不行。Skill 的匹配依赖正则/模糊，建议明确说明匹配是否大小写敏感。
3. **Secret Scan 脚本**：§48 提到 `scripts/secret_scan.py` 是可运行的，但脚本内容没有内联在 SKILL.md 里，需要额外调用 `skill_view(..., file_path="scripts/secret_scan.py")` 才能看到。考虑在 SKILL.md 里加一个"快速验证"片段（哪怕是伪代码/关键代码片段），让用户知道这个脚本能做什么。
4. **无中文注释**：哥哥是中文用户，所有内容都是英文，对新接触的 operator 不够友好（但这可能是设计选择，不是 bug）。

### 适用场景
- Bug Bounty / 渗透测试的侦察阶段
- Red Team 外网资产发现
- 客户资产 attack surface 评估
- 需要快速查某个具体 probe/wordlist/dork 时

### 综合评价
5/5 — 内容深度和广度都非常出色，模块化设计优秀，是目前最实用的 OSINT 实操 skill 之一。

## 相关文件
- 报告日期：2026-08-01
- 测试环境：Linux
- Skill 版本：3.0.0
