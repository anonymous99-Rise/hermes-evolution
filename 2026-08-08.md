# Skills Trial Report — 2026-08-08

**Tester**: Hermes Subagent
**Repo**: adminlove520/hermes-evolution
**Branch**: skills-trials

---

## 试用心得：computer-use

本周对 `computer-use` skill 进行了试用，重点测试浏览器自动化与后台协同能力。

### 测试内容
使用 `computer_use` 工具对 Firefox 浏览器进行后台截图、元素点击、文本输入等操作。

### 体验总结
- **截图可靠性**：SOM 模式（screen overlay with markers）能精确标注可交互元素，比纯坐标点击更可靠
- **后台模式**：默认 background 路由输入不抢占用户焦点，与用户共屏体验流畅
- **层级提升（Escalation Ladder）**：当 background 模式无法确认效果时，按 `confirmed` → `unverifiable` → `suspected_noop` → `foreground` 顺序升级
- **Browser PAGE rung**：针对浏览器内表单输入，使用 `cua_browser_*` 系列 actions 比原生坐标点击更稳定
- **锁机制**：使用 `flock` 防止并发写入同一 worktree，避免提交冲突

### 注意事项
- 不要在 Electron/Chromium 应用上预测需要 escalation，直接按返回信号判断
- 密码框、支付弹窗等高权限 UI 发现后立即停手并询问用户
- `capture` 是唯一无副作用的操作，其他操作需要审批

### 改进建议
- 建议在 SOUL.md 中补充 `computer-use` 的适用场景判断树
- 锁文件路径 `/tmp/hermes-skills-trial.lock` 可考虑改为 `$HOME/.hermes/` 目录

## 相关文件
- 报告日期：2026-08-08
- 测试环境：Linux (5.15.0-69-generic)
- 推送分支：skills-trials
