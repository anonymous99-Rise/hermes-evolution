# Skills Trial Report — 2026-08-15

**Tester**: Hermes Subagent
**Repo**: adminlove520/hermes-evolution
**Branch**: skills-trials

---

## 试用心得：hunt-sqli

### 优点
1. **现代案例丰富**：12个真实案例，覆盖NoSQL injection（Rocket.Chat CVE-2021-22911 MongoDB $regex、Mongoose ORM CVE-2024-53900）、ORM raw-fragment SQLi（Django CVE-2024-42005、Sequelize GHSA）、second-order SOQL injection、GraphQL blind SQLi 等最新变种，不是老掉牙的 `' OR 1=1` 合集。
2. **NoSQL injection 完整覆盖**：MongoDB $where、$regex、$gt 操作符注入，JSON body 注入方式（`{"$where": "..."}`），以及 Unicode 混淆 bypass（`$u0067t`）都有详细说明，对现代 Node.js + MongoDB 栈特别有价值。
3. **WAF bypass 实战技巧**：space 替换（`/**/`、`%09`、`%0A`）、大小写混淆、comment injection（`/*!50000SELECT*/`）、double URL encode、JSON/NoSQL Unicode bypass — 每种都给了可复制的 payload 示例，不是泛泛而谈。
4. **Gate 0 Validation 设计严谨**：用三个问题（攻击者能做什么？受害者损失什么？10分钟内可复现吗？）作为报告门槛，思路清晰能过滤低质量报告，对渗透测试新人很有教育意义。
5. **Related Skills & Chains 完整**：SQLi → RCE（xp_cmdshell/INTO OUTFILE）、SQLi → IDOR（提取 UUID 后枚举）、SQLi → Auth Bypass（password_reset_tokens 表）都有标注，方便形成攻击链。
6. **curl/SQLMap 命令示例实用**：每个检测阶段都有可直接使用的 curl 命令和时间基准测量方法，哥哥用 Windows + curl 验证会非常顺手。

### 缺点 / 可改进
1. **中文注释缺失**：哥哥是中文用户，所有内容都是英文，对新接触 SQL 注入的 operator 不够友好（但这可能是设计选择）。
2. **payload 量巨大难索引**：主文件和 Bypass Techniques 部分 payload 密度很高，建议在 SKILL.md 头部加一个 **Quick Reference Cheatsheet**，把最高频的 10 个 payload（error-based、boolean-blind、time-based、NoSQL 各选代表）做成表格，触发时直接查表不用翻长文。
3. **具体案例细节分散**：12个真实案例以 References 形式分散，需要翻到末尾才能看到详细 Payload 和 Root Cause，建议在每个 Crown Jewel Target 类型下列出最相关的 1-2 个案例作为参考，而不是全部堆在末尾。
4. **PostgreSQL 和 MSSQL 覆盖较弱**：MySQL 部分最详细，但 PostgreSQL（`pg_sleep`、`COPY FROM PROGRAM` → RCE）和 MSSQL（`WAITFOR DELAY`、`xp_cmdshell`）的检测和利用路径可以再深入一些。

### 适用场景
- Bug Bounty / 渗透测试的侦察阶段（URL patterns → injectable parameters 识别）
- Web 安全评估的信息收集阶段（tech stack → payload 选择）
- Red Team 攻击链构建（SQLi → RCE / IDOR / Auth Bypass 串联）
- 快速查表：遇到具体 target 时查对应技术栈的 payload 示例

### 综合评价
5/5 — 覆盖深度和广度都非常出色，NoSQL injection 和 ORM bypass 是亮点，Gate 0 Validation 设计体现了 skill 作者的安全思维成熟度。

## 相关文件
- 报告日期：2026-08-15
- 测试环境：Linux
- Skill 版本：12报告案例（2021-2024）
