# Skills Trial Report — 2026-09-12

**Tester**: Hermes Subagent
**Repo**: adminlove520/hermes-evolution
**Branch**: skills-trials

---

## 试用心得：impeccable

### 技能概述
`impeccable` 是一个专业级前端界面设计与迭代技能，覆盖网站、落地页、仪表盘、产品 UI、组件、表单等一切前端界面工作。包含 UX 审查、视觉层级、信息架构、认知负荷、无障碍、性能、响应式、主题、配色、字体、间距、布局、动效、微交互等全栈维度。

### 试用过程

**1. 定位与入口**

路径：`/root/.hermes/skills/impeccable/`

主入口文件是 `skill/SKILL.src.md`（而非传统的 `SKILL.md`），描述非常详细：
- 完整的 Setup 流程（5步）
- 详细的 Design guidance（配色、排版、布局、动效规则）
- 颜色规则、字体规则、布局规则都有具体数值标准
- 内置 `{{scripts_path}}/context.mjs` 和 `{{scripts_path}}/palette.mjs` 脚本

**2. 目录结构**

```
impeccable/
├── skill/
│   ├── SKILL.src.md    ← 主入口（source of truth）
│   ├── scripts/        ← context.mjs, palette.mjs
│   ├── reference/      ← 各子命令参考文档
│   └── agents/         ← agents 配置
├── skills/             ← 各子命令实现
├── site/               ← 网站
├── tests/              ← 测试
├── cli/                ← 命令行引擎
├── extension/          ← Chrome 扩展
└── (其他工程文件)
```

**3. 子命令体系**

通过 `reference/` 目录支持多个子命令：
- `craft` - 制作
- `shape` - 塑形
- `audit` - 审计
- `polish` - 打磨
- 等等

每个子命令有独立 reference 文档，这是第一个我看到的采用**命令式子技能架构**的 skill。

**4. Setup 流程（5步强制流程）**

```
1. node scripts_path/context.mjs → 读取 PRODUCT.md/DESIGN.md
2. 读 reference/<command>.md（如果用了子命令）
3. 读至少一个项目文件（CSS/tokens/组件）
4. 读 register reference（brand.md 或 product.md）
5. 新项目跑 palette.mjs 获取品牌色
```

这个强制流程保证了设计决策有据可查，不会凭空拍脑袋。

**5. 设计规则库**

SKILL.src.md 里内置了大量设计规则：
- **颜色**：4.5:1 对比度强制、灰字在彩色背景上不能用
- **字体**：行宽 65-75ch、层级对比 ≥1.25、无全大写正文
- **布局**：auto-fit grid、z-index 语义化层级
- **动效**：声明式，动效是设计的一部分不是后补

这些规则都有具体数值，不是模糊建议。

### 优点

- **规则具体可执行**：有具体数值（对比度、字体大小、间距），不是"建议良好"
- **子命令架构清晰**：不同任务类型对应不同 reference，避免一个 skill 塞所有功能
- **Setup 强制执行**：不跑 context.mjs 就不能用，保证上下文感知
- **双色系分离**：brand.md（设计即产品，如营销页）vs product.md（设计服务产品，如仪表盘）
- **Anti-pattern 检测引擎**：`cli/engine/detect-antipatterns.mjs` 是规则引擎，同时支持浏览器和 jsdom 两种检测路径
- **测试覆盖率**：有完整的 fixture + test 体系，甚至有 Playwright E2E 和 LLM 行为测试

### 不足

- **入口文件不是 SKILL.md**：是 `SKILL.src.md`，而 skill_view 默认读 `SKILL.md`，可能造成工具链兼容性问题
- **工程复杂度高**：这是一个完整的 npm 包项目，不是一个轻量 skill，对于只想用设计能力的用户来说太重了
- **子命令 reference 缺失**：目录结构显示了 `reference/` 但我无法确认所有子命令 reference 文档是否存在
- **对非前端任务无能为力**：SKILL 描述里明确说了"Not for backend-only or non-UI tasks"，边界清晰

### 适用场景

- 落地页、营销页设计（品牌类）
- 仪表盘、管理后台设计（产品类）
- UI 组件设计与迭代
- 设计系统建立与审计
- 有具体设计稿需要打磨到生产级别

### 改进建议

- 建议在 skill/SKILL.md（而非 SKILL.src.md）里放一个简洁的索引，说明主入口在 SKILL.src.md
- `impeccable/skills/` 子命令实现目录是空的（0个文件），子命令的实际执行逻辑未找到，可能还需要额外配置
- 建议在 SOUL.md 的 skills 索引中标注 `impeccable` 是"重量级前端 UI 技能"，避免轻量任务误调用

### 结论

`impeccable` 是目前见到最专业、最系统化的前端 UI 设计 skill。规则具体、流程强制、测试完整，但工程复杂度也最高。适合需要严肃做前端设计的场景，不适合临时查一个小问题。

## 相关文件
- 报告日期：2026-09-12
- 测试环境：Linux
- 技能路径：/root/.hermes/skills/impeccable/
- 技能类型：前端 UI 设计（重量级）
